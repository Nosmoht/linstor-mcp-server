@AGENTS.md

Claude-specific notes:

- Treat `AGENTS.md` as the canonical shared contract for this repo.
- Keep edits to this file minimal; prefer updating `AGENTS.md` so Codex and
  Claude stay aligned.
- For complex or cross-cutting changes, use plan mode first when that improves
  safety or reviewability.
- Project-local Claude skills live under `.claude/skills/` and project
  subagents live under `.claude/agents/`.
